{% extends 'base.html.twig' %}

{% block title %}Mon Univers Jeux Vidéo 🎮{% endblock %}

{% block body %}
<style>
  body {
    background: #121212;
    color: #e0e0e0;
    font-family: 'Poppins', sans-serif;
    margin: 0;
    padding: 0;
  }

  .blog-container {
    max-width: 960px;
    margin: 3em auto;
    padding: 30px;
    background: #1e1e1e;
    border-radius: 20px;
    box-shadow: 0 0 30px rgba(0, 0, 0, 0.8);
  }

  .blog-container h1 {
    text-align: center;
    color: #00ffd5;
    font-size: 2.4em;
    margin-bottom: 0.5em;
  }

  .blog-container p {
    text-align: center;
    font-size: 1.1em;
    color: #b0b0b0;
    margin-bottom: 2.5em;
  }

  .articles-list h2 {
    color: #00ffd5;
    border-bottom: 2px dashed #00ffd5;
    padding-bottom: 8px;
    margin-bottom: 20px;
    font-size: 1.6em;
  }

  .article-item {
    background: #232323;
    padding: 20px;
    border-radius: 15px;
    margin-bottom: 20px;
    box-shadow: 0 4px 8px rgba(0,0,0,0.4);
    transition: transform 0.3s ease, background 0.3s ease;
  }

  .article-item:hover {
    transform: translateY(-5px);
    background: #292929;
  }

  .article-item h3 {
    color: #ffe600;
    margin-bottom: 10px;
    font-size: 1.4em;
  }

  .article-item p {
    color: #c0c0c0;
    line-height: 1.6;
    font-size: 1em;
  }

  .article-item a {
    display: inline-block;
    margin-top: 15px;
    text-decoration: none;
    background: linear-gradient(45deg, #00ffd5, #00bfff);
    color: #121212;
    padding: 10px 20px;
    border-radius: 10px;
    font-weight: bold;
    transition: all 0.3s ease;
  }

  .article-item a:hover {
    background: linear-gradient(45deg, #00bfff, #00ffd5);
    color: #000;
  }

  .no-article {
    color: #ff5050;
    font-size: 1.2em;
    text-align: center;
    margin-top: 20px;
  }
</style>

<div class="blog-container">
  <h1>Bienvenue sur mon Blog Jeux Vidéo 🎮✨</h1>

  <p>
    Passionné par l’univers gaming, je partage ici mes articles, tests et coups de cœur.  
    Si tu es fan de jeux vidéo, tu es au bon endroit ! 🚀💡
  </p>

  <div class="articles-list">
    <h2>📢 Les dernières actus :</h2>
    {% for article in articles %}
      <div class="article-item">
        <h3>{{ article.titre }}</h3>
        <p>{{ article.contenu|slice(0, 150) ~ '...' }}</p>
        <a href="{{ path('article_show', { id: article.id }) }}">📖 Lire l'article complet</a>
      </div>
    {% else %}
      <p class="no-article">🚧 Aucun article dispo pour le moment. Reviens vite !</p>
    {% endfor %}
  </div>
</div>
{% endblock %}
